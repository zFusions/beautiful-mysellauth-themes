<div class="container component vl-page-section">
  <section class="py-5-nav">
    <header class="vl-page-head">
      <a href="{{ '/' | shopUrl }}" class="vl-page-back">← Back to store</a>
      <h1>{{ name }}</h1>
    </header>
  </section>
</div>

<div class="components">
  {% for componentId in components_order %}
    {% render_component componentId %}
  {% endfor %}
</div>
