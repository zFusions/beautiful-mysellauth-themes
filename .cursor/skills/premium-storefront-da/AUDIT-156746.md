# Audit — Theme 156746 (Velora Market)

**Date:** 2026-06-13  
**Scope:** Full theme + SellAuth platform alignment

## Executive summary

| Area | Status | Notes |
|------|--------|-------|
| CSS load order | PASS | `pro.css → custom.css → velora.css → shop-pages.css → shop-pdp.css` |
| PDP layout / styling | PASS | `shop-pdp.css` + `product-page.njk`; minimal overlap in `velora.css` |
| Cart page | PARTIAL | Good Velora DA; quantity sync + hardcoded empty-state copy |
| Alpine.js / `productForm` | PASS | Single `x-data="productForm"` on rail |
| `price_slash` | PASS | Variant-level + strikethrough getters |
| Volume discounts (BAMP) | PARTIAL | `product.volume_discounts`; no `disable_volume_discounts_if_coupon` |
| Stock sync | WARN | Static from page load; no live polling |
| Visual editor / resale | FAIL | Hardcoded copy; dead schema fields; snippet `properties` gap |
| Inner-page DA | PARTIAL | Cart/PDP/legal good; maintenance/socials retain legacy blue |
| Platform APIs | PASS | Cart, checkout, altcha, volume_discounts patterns correct |

## Critical issues (P0)

| # | Issue | Location | Fix |
|---|--------|----------|-----|
| C1 | Snippet missing `properties` | `product-page.njk` | Pass `properties=properties` to `product-form.njk` |
| C2 | Dead PDP schema fields | `schema.json` vs `product-page.njk` | Wire `vl-page-head.njk` or remove fields |
| C3 | Cart qty desync on typed input | `cart-page.njk` | Add `@change` → `changeQuantity()` |
| C4 | `meta-tags.njk` uses `.concat()` | Forbidden Nunjucks pattern | Rebuild array without `.concat()` |

## Warnings (P1–P2)

- Hardcoded PDP trust labels (`product-page.njk`)
- Hardcoded cart empty copy (`cart-page.njk`)
- Hardcoded footer copy (`footer.njk`)
- Customer account English strings
- No live stock sync
- `features.njk` / `plants.njk` loops without `default([])`
- Mock JS loaded globally in `master.njk`
- Legacy blue in `maintenance.njk`, `socials.njk`, `text-image-block.njk`
- Stock bar caps at 100% for high stock counts
- Navbar hardcoded `data-component-id="navbar"`

## Platform alignment

**Correct:** `variant.price`, `variant.price_slash`, `variant.stock`, `product.volume_discounts`, `/v1/cart`, `/v1/checkout`, `appCart`, `appCurrency`, `sellauth-theme` CLI.

**Not implemented:** coupon UI on PDP, live stock refresh, `disable_volume_discounts_if_coupon`.

## Recommendations

1. **P0** — Pass `properties` to product-form; fix cart qty sync
2. **P1** — Expose trust row / empty state / footer in `schema.json`
3. **P1** — Fix `meta-tags.njk` `.concat()`
4. **P2** — Gate mock JS to `.velora-home` only
5. **P2** — Migrate legacy blue components to `--vl-*` tokens
